# Changelog (user-facing slice)

Full history: root [`CHANGELOG.md`](../../../CHANGELOG.md).

## Recent (Unreleased on top of 0.3.0)

- `@docs` / `@web` — preview chips and Docs/Web context blocks on send (Rust `http_get_text`).  
- Sprint 11 IDE: Palette (`Ctrl+Shift+P`), Quick Open (`Ctrl+P`), Find, Problems/Outline, Inline Edit (`Ctrl+K`).  
- Agent action algorithm in the system prompt + **Strict tools** (nudge-retry for weak local models).  
- Settings → **Rules & RAG**; Pin → Success RAG and **📌 AGENTS**.  
- Multi-LSP, ghost-text, Sync models, MCP stdio pipe, signed updater (own endpoint/keys = ops).  
- Docs: unified TZ v4.1 + `docs/publish/` pack (RU/EN); “How it was built” section in publish READMEs.

## 0.3.0

- Layout Split / Chat / Editor · `git_commit` · parallel read-only tools  
- Settings export/import · soft update check · release workflow  
- Subagent-lite · Success RAG hybrid · ToolCallView v2 · 👍/👎  
- Compact health · modular settings · MSI/NSIS 0.3.0  

## 0.2.0

- Projects · editable Monaco · `@file` / `@codebase` · checkpoints · Ask/Agent/Plan · MCP HTTP · MSI 0.2.0
